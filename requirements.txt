# Morning Motivation Quote Generator - Python Dependencies
# Install with: pip install -r requirements.txt
#
# SECURITY: Versions are pinned to specific releases to prevent
# installation of potentially vulnerable future versions.
# Update periodically and test after updating.

# Required for all versions (V1.0.0+)
# HTTP requests for API calls
# https://pypi.org/project/requests/
requests==2.32.3

# Required for V5.0.0+ (Advanced Visual Design)
# Image processing for gradient backgrounds
# https://pypi.org/project/Pillow/
Pillow==11.0.0
